{
  "name": "vault",
  "version": "1.0.0",
  "description": "A robust smart contract system for token locking with linear decay voting power mechanics",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-sepolia": "npx hardhat run deployments/deploy_vault.js --network sepolia",
    "compile": "npx hardhat compile"
  },
  "author": "frankfourier.eth",
  "license": "MIT",
  "keywords": [
    "ethereum",
    "base",
    "smart-contracts",
    "solidity",
    "vault",
    "defi",
    "voting-power",
    "token-locking"
  ],
  "devDependencies": {
    "@nomiclabs/hardhat-etherscan": "^3.1.8",
    "hardhat": "^2.22.1"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-verify": "^2.0.6",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@openzeppelin/contracts": "^5.0.2",
    "@uniswap/v2-core": "^1.0.1",
    "dotenv": "^16.4.5",
    "ethereumjs-util": "^7.1.5"
  }
}